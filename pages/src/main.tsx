import React from 'react'
import ReactDOM from 'react-dom/client'
import { BacktestApp } from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BacktestApp />
  </React.StrictMode>
)
