# Release v0.27.0 - Four Pillars Fund Categories

Released: 2026-01-19

## Overview

This release introduces the Four Pillars portfolio categorization system, enabling users to classify funds by investment philosophy: Liquidity, Yield, Store of Value (SoV), and Volatility. Margin is tracked separately as borrowing capacity. Also includes fixes for timezone issues in holiday detection and CI improvements.

## New Features

### Four Pillars Fund Categories
Portfolio categorization system with 4 investment philosophy pillars, plus margin tracking:

- **Liquidity** - 24/7 cash access for spending and investing (Cash, Money Market)
- **Yield** - High yield cash storage at 10-11% APY (STRC, high-yield ETFs)
- **Store of Value (SoV)** - Global store of value, hedge against fiat (BTC, ETH)
- **Volatility** - Capture whole market fluctuations (TQQQ, SPXL, UPRO)
- **Margin** - Tracked separately as borrowing capacity, not an allocation category

**Features:**
- New `FundCategory` type with 4 categories and configuration
- Category selector in Create/Edit fund modals (hidden for cash funds - always liquidity)
- Horizontal bar chart on dashboard showing portfolio allocation by category
- Margin capacity indicator (available vs borrowed) shown separately as overlay
- Category badge on fund cards (Liq, Yld, SoV, Vol)
- Auto-assign categories: cash→liquidity, derivatives→volatility, btc→sov, strc→yield
- Philosophy docs updated with "The Four Pillars of Portfolio Construction" section

### Automatic Build Number Incrementing
- CI now automatically increments the patch version on every push to the dev branch
- Version format: `MAJOR.MINOR.BUILD` (e.g., `0.27.1`, `0.27.2`, etc.)
- Build numbers increment after tests pass, providing traceable version numbers for each dev commit
- Commits with `[skip ci]` in the message are excluded from triggering builds

## Bug Fixes

### Holiday Detection Timezone Fix
- Fixed timezone issue in holiday detection that caused incorrect results in non-UTC timezones
- The `isUSMarketHoliday` function was using local time methods on a UTC date
- Holiday calculations now consistently use UTC methods to match US Eastern Time normalization

## Technical Details

### Files Changed
- `packages/engine/src/types.ts` - Added FundCategory type
- `packages/engine/src/fund-type-config.ts` - Added FUND_CATEGORY_CONFIG with labels, colors, shortLabels
- `packages/web/src/components/CategoryBarChart.tsx` - New horizontal bar chart with margin overlay
- `packages/web/src/components/PortfolioCharts.tsx` - Integrated category bar chart in 3-column layout
- `packages/web/src/pages/Dashboard.tsx` - Category allocation computation
- `packages/web/src/components/CreateFundModal.tsx` - Category selector with auto-assign
- `packages/web/src/components/EditFundPanel.tsx` - Category selector (hidden for cash funds)
- `packages/web/src/components/FundCard.tsx` - Category badge display
- `docs/philosophy.md` - Added Four Pillars documentation
- `packages/web/src/utils/format.ts` - UTC methods for holiday detection
- `.github/workflows/ci.yml` - Added bump-build job

## Installation

```bash
git clone https://github.com/atomantic/EscapeMint.git
cd EscapeMint
npm run setup
npm run dev
```

## Full Changelog

**Full Diff**: https://github.com/atomantic/EscapeMint/compare/v0.26.0...v0.27.0
