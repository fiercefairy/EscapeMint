# Release v0.28.0 - Dashboard Charts & Derivatives Fix

Released: 2026-01-20

## Overview

This release enhances the main portfolio dashboard with improved charts, adds a confirmation dialog when adding entries to closed funds, and fixes a critical bug in the derivatives liquidation price calculation.

## Bug Fixes

### Derivatives Liquidation Price Calculation
Fixed incorrect liquidation price calculation for BTC perpetual futures:

- **Root Cause**: The maintenance margin rate was set to 5% instead of the correct 20% for Coinbase BTC perpetuals
- **Symptom**: Liquidation prices were showing negative values (e.g., -$7,997) instead of realistic positive values (e.g., ~$7,500)
- **Fix**: Updated `DEFAULT_MAINTENANCE_MARGIN_RATE` from 0.05 to 0.20 and made it configurable per fund via the `maintenance_margin_rate` config field

### Files Changed
- `packages/engine/src/derivatives-calculations.ts` - Updated default rate and added parameter
- `packages/engine/src/types.ts` - Updated documentation for maintenance_margin_rate field
- `packages/server/src/routes/funds.ts` - Pass maintenance margin rate from config (6 locations)
- `packages/server/src/services/dashboard-cache.ts` - Pass maintenance margin rate from config
- `packages/server/src/utils/fund-metrics.ts` - Pass maintenance margin rate from config

## New Features

### Dashboard Chart Improvements

Enhanced the main portfolio dashboard with better visualizations:

- **Stacked Liquid Value Chart**: Converted the "Fund Liquid Value" chart from a simple area chart to a stacked area showing Cash (bottom, emerald) + Asset Value (top, violet) for a complete picture of liquid holdings
- **Unrealized APY Line**: Added unrealized APY (orange) to the APY chart alongside realized APY (emerald), calculated as `liquidAPY - realizedAPY`
- **Value & Allocation Chart**: Added aggregate chart showing:
  - Asset Value (orange line)
  - Target threshold (cyan dashed line) - shows where you'd start selling
  - Invested amount (purple area)
- **Reorganized Layout**: Moved Portfolio Allocation chart to top row for better visibility; removed redundant Asset Allocation and standalone Cash charts

### Closed Fund Reopen Confirmation
When adding an entry to a fund with status "closed", users are now prompted to confirm reopening:

- **Confirmation Dialog**: "This fund is currently closed. Adding an entry will reopen it. Continue?"
- **Reopen & Save**: Confirms the entry and automatically sets fund status to "active"
- **Cancel**: Dismisses the dialog and stays on the entry form without saving
- **Toast Notification**: Shows "Entry added and fund reopened" on successful save

This improves the UX when revisiting closed positions or resuming trading in a previously closed fund.

## Technical Details

### Dashboard Charts Files Changed
- `packages/web/src/components/PortfolioCharts.tsx` - Added `LiquidValueStackedChart`, `PortfolioValueAllocationChart` components; added unrealized APY to `APYChart`; reorganized chart layout
- `packages/web/src/api/funds.ts` - Added `totalExpectedTarget` to `TimeSeriesPoint` interface
- `packages/server/src/routes/funds.ts` - Added `totalExpectedTarget` calculation in history endpoint using `computeExpectedTarget` from engine

### Closed Fund Reopen Files Changed
- `packages/web/src/components/AddEntryModal.tsx` - Added confirmation dialog logic and state management
- `packages/web/src/pages/FundDetail.tsx` - Passes `fundStatus` prop to AddEntryModal

### Implementation
- Added `fundStatus` prop to `AddEntryModalProps` interface
- Added `showReopenConfirm` and `pendingEntry` state for dialog management
- Refactored `handleSubmit` to intercept closed fund submissions
- Created `saveEntry` helper for normal and reopen saves
- Uses existing `ConfirmDialog` component for consistent UI
- Calls `updateFundConfig` API to set status to "active" on confirmation

## Installation

```bash
git clone https://github.com/atomantic/EscapeMint.git
cd EscapeMint
npm run setup
npm run dev
```

## Full Changelog

**Full Diff**: https://github.com/atomantic/EscapeMint/compare/v0.27.0...v0.28.0
